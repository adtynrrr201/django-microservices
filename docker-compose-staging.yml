version: "3.9"

services:
  auth_service:
    image: ${DOCKERHUB_USERNAME}/auth_service:latest
    container_name: auth_service_staging
    restart: always
    env_file:
      - ./envs/staging/auth.env
    depends_on:
      - postgres
    ports:
      - "8001:8000"
    networks:
      - backend

  order_service:
    image: ${DOCKERHUB_USERNAME}/order_service:latest
    container_name: order_service_staging
    restart: always
    env_file:
      - ./envs/staging/order.env
    depends_on:
      - postgres
    ports:
      - "8002:8000"
    networks:
      - backend

  postgres:
    image: postgres:16
    container_name: postgres_staging
    restart: always
    environment:
      POSTGRES_DB: coba
      POSTGRES_USER: aditya
      POSTGRES_PASSWORD: adityanur
    volumes:
      - pgdata_staging:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - backend

volumes:
  pgdata_staging:

networks:
  backend:
